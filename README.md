# devops-next-gen

### Create Kind `dev` cluster
```
cd kind-terraform
./dev.sh
terraform destroy -auto-approve
```

### Delete Kind `dev` cluster
```
cd kind-terraform
terraform destroy -auto-approve
```